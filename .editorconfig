# top-most EditorConfig file
root = true

# Unix-style newlines with a newline ending every file
[*.{java, kt, kts, md, xml, aidl, gradle, properties, json, yml, yaml}]
end_of_line = lf
insert_final_newline = true
